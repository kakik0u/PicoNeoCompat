# Third-party notices

`include/nvEncodeAPI.h` is the NVIDIA NVENC API header from the
`FFmpeg/nv-codec-headers` project, tag `n11.1.5.2`. Its copyright and MIT-style
permission notice are retained at the top of that file.
